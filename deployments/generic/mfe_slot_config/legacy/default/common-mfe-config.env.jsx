/**
 * Generic Open edX MFE common slot configuration for the "default" deployment.
 *
 * This file configures shared slot plugins for all non-learning MFEs.
 * Replace with operator-specific slot configuration as needed.
 */

const config = {};

export default config;

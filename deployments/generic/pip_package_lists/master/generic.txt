# Generic Open edX deployment — beyond edx-platform's own requirements, only the
# packages the lehrer aqueduct settings framework needs at runtime.
# The base requirements (requirements/edx/base.txt and requirements/edx/assets.txt)
# are pulled automatically from edx-platform by the lehrer build pipeline.
#
# To add custom packages, list them here one per line (pip requirements format).
# Then run `dagger call platform regenerate-aqueduct-settings` to update the
# settings models in settings/lms/models/aqueduct.py and settings/cms/models/aqueduct.py.

# Required by the generated lms/cms aqueduct settings modules
# (lms.envs.aqueduct imports django_aqueduct; the settings models import
# pydantic_settings.YamlConfigSettingsSource, which needs the [yaml] extra).
django-aqueduct==0.6.0
pydantic-settings[yaml]==2.14.2

/**
 * Generic Open edX learning MFE slot configuration.
 *
 * This file configures the slot plugins for the learning MFE.
 * Replace with operator-specific slot configuration as needed.
 */

const config = {};

export default config;
